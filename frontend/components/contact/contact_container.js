import { connect } from 'react-redux';

import Contact from './contact';

const mapDispatchToProps = (dispatch) => ({

});

export default connect(null, mapDispatchToProps, null, { forwardRef: true })(Contact);
